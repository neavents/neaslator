# GENERATED by k8s/generate.py in the estate root — edit k8s/services.json, not this file.
apiVersion: v1
kind: Service
metadata:
  name: neaslator
  # See the namespace note on the Deployment: pinned so a stray apply cannot create a second,
  # broken copy of this estate in another namespace.
  namespace: neavents
  labels:
    app.kubernetes.io/name: neaslator
    app.kubernetes.io/part-of: neavents
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: neaslator
  ports:
    - name: http
      port: 80
      targetPort: 5300
