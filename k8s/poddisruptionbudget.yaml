# GENERATED by k8s/generate.py in the estate root.
#
# Voluntary disruption — a node drain, a cluster upgrade — must never take the last replica.
# With replicas: 2 this permits exactly one at a time, which is what makes a node drain safe
# rather than an outage.
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: neaslator
  namespace: neavents
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: neaslator
