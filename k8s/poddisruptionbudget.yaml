# GENERATED by k8s/generate.py in the estate root — edit k8s/services.json, not this file.
#
# Voluntary disruption — a node drain, a cluster upgrade — must never take the last replica.
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: neaslator
  # See the namespace note on the Deployment.
  namespace: neavents
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: neaslator
